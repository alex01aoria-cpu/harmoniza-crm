export type DashboardSummary = { leads_total:number; qualificados:number; agendamentos:number; compras:number; ticket_medio:string|null; perdas:number; perdas_por_motivo:Record<string,number>; pipeline:Record<string,number>; };
export function SummaryCards({ summary }: { summary: DashboardSummary | null }) { const cards=[['Leads', summary?.leads_total ?? '--'],['Qualificados', summary?.qualificados ?? '--'],['Agendamentos', summary?.agendamentos ?? '--'],['Compras', summary?.compras ?? '--'],['Ticket médio', summary?.ticket_medio ?? '--'],['Perdas', summary?.perdas ?? '--']]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([label,value])=><div key={label} className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><p className="text-sm text-zinc-500">{label}</p><p className="mt-3 text-3xl font-semibold text-zinc-950">{value}</p></div>)}</div>; }
