export function OutcomeForm({ leadId }: { leadId: number }) { return <section className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold text-zinc-950">Resultado comercial</h2><p className="mt-2 text-sm text-zinc-600">Resultado e motivo de perda disponíveis via API: <code>/leads/{leadId}/outcome</code> e <code>/leads/{leadId}/loss-reason</code>.</p></section>; }
